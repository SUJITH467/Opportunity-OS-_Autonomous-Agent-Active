# Secrets (local environment files are never committed)
.env.local

# CDK Build Artifacts
cdk/cdk.out/
cdk/node_modules/
cdk/dist/

# Built ZIP artifacts
*.zip

# CLI Internals
.cli/*

# Ephemeral Staging
.cache/*
opportunityos_orchestrator/

# Exception: Commit the State
!.cli/deployed-state.json
